% 
%  pja-article.sty
%  Sty-files
%  
%  Created by Maarten Steenhagen on 2012-09-06.
%  Copyright 2012 Maarten Steenhagen. All rights reserved.
% 

% Here we load all used packages.
\usepackage{lettrine} % Drop-caps
\usepackage{graphics} 
\usepackage{color} 
\usepackage{clock} 
\usepackage{epigraph}
\usepackage[colorlinks=true,linkcolor=black,urlcolor=black,citecolor=black]{hyperref}
\usepackage[absolute]{textpos}
\usepackage[british]{babel}
\usepackage{draftwatermark}
\usepackage{fontspec}
\usepackage{xltxtra}
\usepackage{xunicode}
\usepackage{titlesec}
\usepackage[hang]{footmisc}
\usepackage{setspace}
\usepackage[top=2.5cm, bottom=2.5cm, left=2.7cm, right=2.7cm]{geometry} 
\usepackage{fancyhdr}

% Here we set the page margins
\topmargin -0.5cm 
\parindent 0.6cm 
\textheight 23cm 
\parskip 0.2mm 
\linespread{1.2} 
\oddsidemargin 0cm 
\evensidemargin 0cm

% Here we define what is needed for our bibliography.
\usepackage[style=authoryear-ibid, ibidtracker=constrict, autocite=footnote]{biblatex}
\bibliography{/Users/msteenhagen/Dropbox/Own-Work/BIBLIOGRAPHY/BibDesk/books}

% Here we define the colour 'grey'
\definecolor{grey}{cmyk}{0,0,0,0.8}

% Here we define a command that embeds a watermark. 
\newcommand{\proof}{\SetWatermarkText{Uncorrected Proof}\SetWatermarkLightness{0.95}\SetWatermarkScale{0.7}}
% We can now add '\proof' in our preable.

% Here we define a command that writes pdf-information. 
\newcommand{\pdfdetails}{\hypersetup{pdftitle = {\journal}, pdfauthor= {\author}, pdfsubject= {\journalbrief ~\volume (\issue)~\year}}}
% We can now add '\pdfdetails' in our document.

% Here we define a command that sets the page-number of the first page.
\newcommand{\firstpage}[1]{\setcounter{page}{#1}}
% We can now add '\firstpage[13]' to our preable.

% Here we define a dropcap command.
\newcommand{\dropcap}[2]{\lettrine[lines=3, lhang=0.00, loversize=0.00]{{\pfancy #1}}{{\large #2}}}
% We can now use '\dropcap{A}{ll your base} are belong to us.
\setlength{\skip\footins}{0.7cm}



% Here we define the fonts used. Brill is set as main font.
\defaultfontfeatures{Scale=MatchLowercase,Mapping=tex-text}
\setmainfont[Scale=0.95, Contextuals={WordInitial,WordFinal}, Ligatures={Common}]{Brill}
\setmonofont[Scale=1.00,Ligatures=NoCommon]{Courier}
\newfontface\fancy [Contextuals={WordInitial,WordFinal}]{Minion Pro}
\newfontface\fancyb [Contextuals={WordInitial,WordFinal}]{Brill}
\newfontface\fancyi [Contextuals={WordInitial,WordFinal}]{Brill}
\newfontface\pfancy [Contextuals={WordInitial,WordFinal}]{Hoefler Text}
% Note that two commands for Greek are defined as well.
\newfontfamily{\greekscr}[Numbers={OldStyle}, Alternate=1,Contextuals={Inner, Swash,WordFinal}, Ligatures={Rare,Historical}, Script=Greek,Language=Greek]{Brill}
\newfontfamily{\greek}[Ligatures={Rare,Common}, Script=Greek,Language=Greek]{Brill}

% This defines the way section headings appear
\titleformat{\section}{\LARGE\fancyb}{\thesection}{0.5em}{}


% This defines our footnotes
\renewcommand{\hangfootparindent}{1.5em}
% Indentation for 2nd etc. paragraphs in footnotes which cosists of more than one paragraph
\renewcommand{\hangfootparskip}{3pt}
% Vertical space between paragraphs in multiparagraph footnotes
\renewcommand{\footnotemargin}{0.00001pt}
% Setting left margin; this is the smallest value I can get to have second etc. lines indented to footnote number; zero put indentation to some positive value, and negative values do not help actually
\renewcommand{\footnotelayout}{\hspace{1.5em}}
% Here you can modify the spacing between the footnote number and the text of footnote; keep this value and \hangfootparindent value the same

% This sets the general page style
\pagestyle{fancy}

% This defines the page style for the title page
\newcommand{\ps@titlepg}{
   \renewcommand{\@oddhead}{~}%     header blank
      \renewcommand{\@evenhead}{~}%    header blank
      \renewcommand{\@oddfoot}{\footnotesize{\copyright ~ \author ~ \year \\ \href{\journalinfo}{\journalinfo}}}%     
      \renewcommand{\@evenfoot}{\footnotesize{\copyright ~ \author ~ \year \\ \href {\journalinfo}{\journalinfo}}}%
}

% This defines the page style for subsequent pages
\lhead{}
\chead{}
\rhead{\textsc \author}
\lfoot{~}
\cfoot{~}
\rfoot{\thepage}


%Various own commands
\newcommand{\standardbib}{\printbibliography}

% Here we define a margin comment command.
\newcommand{\cmt}[1]{\marginpar {\begin{spacing}{0.8} \begin{flushleft} \textcolor{red}{\textbf {Note:}}~ {\small \emph{#1}}\end{flushleft}\end{spacing}}}
% Now we can use '\cmt{text}' to print a comment.

% Defines the commands for author information etc.
\newcommand{\thetitlex}[1]{\newcommand{\totle}{#1}}
\newcommand{\thehtitlex}[1]{\newcommand{\htitle}{#1}}
\renewcommand{\theauthor}[1]{\renewcommand{\author}{#1}}
\newcommand{\theaffilx}[1]{\newcommand{\affil}{#1}}
\newcommand{\thecontactdx}[1]{\newcommand{\contactd}{#1}}
\newcommand{\theemailx}[1]{\newcommand{\email}{#1}}
\newcommand{\thejournalx}[2]{\newcommand{\journal}{#1} \newcommand{\journalbrief}{#2}}
\newcommand{\thevolumex}[1]{\newcommand{\volume}{#1}}
\newcommand{\theissuex}[1]{\newcommand{\issue}{#1}}
\newcommand{\themonth}[1]{\renewcommand{\month}{#1}}
\newcommand{\theyear}[1]{\renewcommand{\year}{#1}}
\newcommand{\thejournalinfo}[1]{\newcommand{\journalinfo}{#1}}
\newcommand{\thetitle}[1]{\renewcommand{\totle}{#1}}
\newcommand{\thehtitle}[1]{\renewcommand{\htitle}{#1}}
\newcommand{\theaffil}[1]{\renewcommand{\affil}{#1}}
\newcommand{\thecontactd}[1]{\renewcommand{\contactd}{#1}}
\newcommand{\theemail}[1]{\renewcommand{\email}{#1}}
\newcommand{\thejournal}[2]{\renewcommand{\journal}{#1} \renewcommand{\journalbrief}{#2}}
\newcommand{\thevolume}[1]{\renewcommand{\volume}{#1}}
\newcommand{\theissue}[1]{\renewcommand{\issue}{#1}}
% Now these commands for author information etc. are set to blank.
\thetitlex {~} % Fist page (no caps is fine)
\thehtitlex{~} % Title in the header (no caps is fine)
\theauthor{~}
\theaffilx{~}
\thecontactdx{~}
\theemailx{~} 
\thejournalx{~}{~} % Left corner subsequent pages
\thevolumex{~}
\theissuex{~}
\themonth{~}
\theyear{~}
% We need to renew these in our preamble.

% Here we define some font styles needed for the title
\newcommand{\FAT}{\fontsize{35}{30}\selectfont}
\newcommand{\FRONTFAT}{\fontsize{40}{60}\selectfont}
\newcommand{\pg}{{\pfancy \S}} % For fancy paragraph symbols §

% Here we define the article title command.
\renewcommand{\maketitle}[1]{
\clearpage
\setcounter{footnote}{0}
\setcounter{section}{0}
\lfoot{~}
\thispagestyle{titlepg}
\addcontentsline{toc}{part}{\author,~~`\htitle'}
\noindent \emph{\journal , Vol. \volume, No. \issue, ~ \month ~ \year}, pp. \pageref{\author}-\pageref{\email}.\\[2cm]
\label{\author}
\noindent \FAT{\textsc{#1}}\\[1cm]
\noindent\LARGE{\author}\\
\noindent\Large{\emph{\affil}}\\[1.5cm]~}

% Here we define the command that closes the article.
% This prints author information, and also prints the bibliography.
\newcommand{\signoff}[1]{
{\flushright \textsc{\affil} \\ \email\\[1cm]}
\subsubsection*{About the author}\footnotesize{#1}\\[1cm]
\standardbib
\lfoot{\footnotesize{\copyright ~ \author~---~\emph{\journal}~\volume(\issue), \year. pp. \pageref{\author}-\pageref{\email}.}}\label{\email}\clearpage}

\renewcommand{\headrulewidth}{0.0pt}
\renewcommand{\footrulewidth}{0.0pt}

